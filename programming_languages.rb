def reformat_languages(languages)
  # your code here
  array = []
  new_hash = {}
  languages.each do |language_hash, language|
    if language_hash == :oo 
      value.each do |key, value|
        new_hash[:key] = "#{value}"
        
      end
    end
  end
new_hash 
end
